package NewViews;

import android.content.Context;

import com.example.viewpagertry2.DBManager;
import com.example.viewpagertry2.FinalWordProperties;

public class SortingButton extends WordButton {
    public SortingButton(Context context, FinalWordProperties finalWordProperties, DBManager activeDbManager) {
        super(context, finalWordProperties, activeDbManager);
    }
}
